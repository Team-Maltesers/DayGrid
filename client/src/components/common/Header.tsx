import React from "react";
import classes from "../../styles/common/Header.module.css";

const Header = (): JSX.Element => {
  return <div className={classes.header__bg}></div>;
};

export default Header;
